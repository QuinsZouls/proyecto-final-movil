import React from 'react';
import Layout from '../components/Layout';
const Home: React.FC = () => {
  return (
    <Layout noHeader>
      <div>Home courses</div>
    </Layout>
  );
};

export default Home;
